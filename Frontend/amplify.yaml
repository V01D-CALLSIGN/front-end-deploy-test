version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd my-app
        - rm -rf node_modules package-lock.json
        - npm install
    build:
      commands:
        - cd my-app
        - npm run build
  artifacts:
    baseDirectory: my-app/dist
    files:
      - '**/*'